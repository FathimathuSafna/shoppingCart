<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="path/to/bootstrap.min.css"> <!-- Ensure Bootstrap CSS is included -->
  <link rel="stylesheet" href="path/to/icons.css"> <!-- Ensure icon styles are included -->
  <title>Volt-Cart</title>
  <style>
    body {
      background: var(--bg-color);
      color: var(--text-color);
      padding-bottom: 80px;
      padding-top: 80px;
    }

    nav {
      position: fixed;
      top: 0;
      right: 0;
      width: 100%;
      z-index: 10000;
      background: var(--bg-color);
      box-shadow: 0 1px 6px 0 rgb(32 33 36 / 5%);
      padding: 18px 14%;
      align-items: center;
      justify-content: space-between;
      transition: all .6s;
    }

    .navbar-nav {
      flex: 1; /* Allows centering by taking up remaining space */
    }

    .navlist {
      display: flex;
      justify-content: center; /* Centers nav items */
      gap: 15px;
    }

    .navlist a {
      display: inline-block;
      margin: 0;
      font-size: var(--p-font);
      font-weight: 500;
      color: var(--text-color);
      transition: all .6s;
    }

    .logo {
      font-size: 1.5rem;
      font-weight: bold;
      text-transform: uppercase;
      color: var(--text-color);
      
    }

    .logo:hover, .logo:focus {
      color: inherit;
      text-decoration: none;
    }

    .navlist a:hover {
      transform: translate(-4px);
      color: var(--second-color);
    }

    .nav-right {
      display: flex;
      align-items: center;
      gap: 10px;
      padding-left: 20rem;
    }
    
    .d-flex a {
      display: inline-block;
      font-size: 22px;
      color: var(--text-color);
      transition: all .6s;
    }
    

    .nav-right a:hover {
      transform: translate(-5px) scale(1.1);
    }

    #menu-icon {
      font-size: 35px;
      color: var(--text-color);
      cursor: pointer;
    }

    .dropdown-menu .dropdown-item {
      font-size: 18px;
    }

    @media (max-width: 991px) {
      nav {
        padding: 18px 5%;
      }

      .navlist {
        flex-direction: column;
        align-items: center;
        margin-top: 10px;
      }

      .navlist a {
        margin: 10px 0;
      }

      .nav-right {
        flex-direction: column;
        align-items: center;
        margin-top: 10px;
      }
      
      .nav-right a {
        margin: 5px 0;
      }
    }
    .logo {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* Modern, clean font */
      font-size: 2rem; /* Larger size for better visibility */
      font-weight: 600; /* Semi-bold for prominence */
      color: #212529; /* Dark color for contrast */
      text-transform: uppercase; /* Clean, standard look */
      letter-spacing: 1.5px; /* Slightly increased spacing */
      text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
      transition: color 0.3s ease, text-shadow 0.3s ease; /* Smooth transition for hover effect */
    }

    .logo:hover {
      color: #0056b3; /* Bootstrap primary color for hover effect */
      text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.2); /* Enhanced shadow on hover */
      text-decoration: none; /* Ensure no underline */
    }
       @media (max-width: 576px) {
  .logo{
    font-size: larger;
    padding-bottom: 0%;
  }
  }
  .col-3{
    padding-left: 0;
  }
  #cart-count {
    position: absolute;
    top: -8px;
    right: -10px;
    font-size: 10px; /* Make the cart count smaller */
    padding: 0.2rem 0.5rem;
    background-color: red; /* Optional: change the badge color */
  }

  /* On smaller screens, adjust the position */
  @media (max-width: 576px) {
    #cart-count {
      top: -10px;
      right: -5px;
    }
  }
  
  </style>
</head>
<body>
  {{#if showHeader}}
  <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">

   <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
        <a href="/" class="navbar-brand logo col-3 px-1 pr-1" >Volt-Cart</a>



    <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
      <ul class="navbar-nav navlist">
        <li class="nav-item">
          <a class="nav-link" href="/view-all-products">Products</a>
        </li>
        {{#if user}}
        <li class="nav-item">
          <a class="nav-link" href="/orders">Orders</a>
        </li>
        {{/if}}
        <li class="nav-item">
          <a class="nav-link" href="#">About</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Terms & Conditions</a>
        </li>
      </ul>
       </div>

      <div class=" col-5 col-lg-2 col-md-2   d-flex align-items-center  px-1  ">
        <a href="#"><i class="ri-search-line " style="padding-right: 0.2rem; padding-left:1rem"></i></a>
        <a href="/cart">
          <i class="ri-shopping-cart-line" style="padding-left: 0.4rem;"></i>
          <sup><span class="badge bg-success" id="cart-count">{{cartCount}}</span></sup>
        </a>
            <li class="nav-item dropdown" style="list-style-type: none">
  <a class="nav-link dropdown-toggle black"  href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    {{#if user}}
    <i class="ri-account-circle-line text-dark"></i>
      <span class="username mr-2" style="font-size:medium ">{{user.name}}</span>
    {{else}}
      <i class="ri-user-line"></i>
    {{/if}}
  </a>
  <div class="dropdown-menu" aria-labelledby="navbarDropdown">
    {{#if user}}
      <a class="dropdown-item" href="/logout">Logout</a>
    {{else}}
      <a class="dropdown-item" href="/login">Login</a>
    {{/if}}
  </div>
</li>

    </div>
  </nav>
  {{/if}}
 
</body></html>

<style>
  @media (max-width: 576px) {
  .logo{
    font-size: larger;
    padding-bottom: 0%;
  }
  }

</style>
